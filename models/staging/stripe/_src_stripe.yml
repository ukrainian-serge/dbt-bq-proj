version: 2

sources:
  - name: stripe
    description: "Raw data from the Stripe API"
    database: raw-data-503215   # The Project ID
    schema: stripe         # The Dataset name
    tables:
      - name: payment
        config:
          freshness:
            warn_after: {count: 24, period: hour}
            # error_after: {count: 48, period: hour}

          loaded_at_field: _batched_at